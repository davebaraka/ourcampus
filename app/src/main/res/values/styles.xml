<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="titleTextColor">@color/colorAccent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:colorEdgeEffect">@color/colorDefault</item>
        <item name="colorControlHighlight">@color/colorDefault</item>
        <item name="android:homeAsUpIndicator">@drawable/ic_arrow_back</item>
    </style>

    <style name="ProgressBarStyle">
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

    <style name="LaunchTheme" parent="AppTheme">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:statusBarColor">@color/colorAccent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@drawable/spalsh_screen</item>
    </style>

    <style name="LoginTheme" parent="@style/FirebaseUI.AuthMethodPicker.ButtonHolder">
        <item name="android:statusBarColor">@color/fui_transparent</item>
        <item name="android:layout_marginBottom">64dp</item>
        <item name="android:textColor">@color/black</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@drawable/spalsh_screen</item>
    </style>

    <style name="AccentSelector" parent="AppTheme">
        <item name="colorControlHighlight">@color/colorPrimary</item>
    </style>

    <style name="SelectableItemDefault" parent="AppTheme">
        <item name="colorControlHighlight">@color/colorDefault</item>
    </style>

    <style name="BottomNavigationView" parent="Widget.Design.BottomNavigationView">
        <item name="android:textStyle">bold</item>
    </style>

</resources>

